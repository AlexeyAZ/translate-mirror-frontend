import React from 'react';
import PropTypes from 'prop-types';

const Layout = props => {
  return (
    <header className="">
      
    </header>
  );
};

Layout.propTypes = {
  
};

export default Layout;